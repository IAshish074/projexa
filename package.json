{
  "name": "taskflow-mern",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm install && npm run build && cd ../ && rm -rf server/public && mkdir -p server/public && mv client/dist/* server/public/ && cd server && npm install",
    "start": "cd server && npm start",
    "dev": "concurrently \"npm run dev-server\" \"npm run dev-client\"",
    "dev-server": "cd server && npm run dev",
    "dev-client": "cd client && npm run dev"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
